# 2026-09-06 工作日志

## 标题栏回退为系统原生（撤销 v1.0.1 自定义 JS 拖动方案）（内部 2609060002）
- 用户反馈：v1.0.1 的 JS 受控拖动（`bindTitleDrag` + 后端 `window_move`）在 pywebview edgechromium 下"故障更严重了"，要求把顶部栏恢复到 2609050016 的状态。
- 关键事实：2609050016 早于自定义无边框标题栏（该栏在 2609050018 才引入），当时用的是**系统原生标题栏**（`frameless=False`，无自定义 `#titlebar`、无三圆点）。所以"恢复到 2609050016 的标题栏"= 恢复原生标题栏。
- 用户选择"仅标题栏改回原生"（保留 2609050017 之后的所有功能，包括 Bug2 概览每秒刷新 / Bug3 图片稳健下载）。
- 改动：`app.py` `frameless=True` → `frameless=False`；`ui/index.html` 删除自定义 `<header id="titlebar">` 三圆点栏；`ui/app.js` 删除 `let DRAG`、`bindTitleDrag()` 调用及其函数、以及 `windowMinimize/windowToggleMaximize/windowClose` 函数（这些仅服务于自定义无边框栏）。`refreshOverviewLive` 的 setInterval 保留。
- 验证：`node --check ui/app.js` 通过；`py_compile app.py` 通过；build.py 用 weibobot venv 重打包至 2609060002（v1.0.1）。
- 备份：build.py 自动把上一版 2609060001 归档到 `history/微博bot小助手_2609060001.exe`，可随时恢复。清理根目录残留 `.spec`。README 版本迭代记录表首行追加 2609060002 一行（说明标题栏回退原生）。
- 结论：窗口拖动现在由系统原生标题栏负责，主体区域文本可自由选中/复制，彻底消除"拖动 vs 选词"冲突；且不损失任何后续功能。

## UI 细节调整（内部 2609060003）
- 需求：① 概览改为系统概览；② 隐藏品牌区内部版本号 `(内部 2609060002)`；③ 点击 `Copyright 2026 肆月Aperture` 跳转到 `https://github.com/zouzuo1994321/Weibo_Bot`。
- 改动：`ui/index.html` 左侧导航项「概览」→「系统概览」；概览页 `<h2>` 同步改为「系统概览」；侧边栏底部 `#copyLabel` 增加 `cursor:pointer` 和 `title` 提示。`ui/app.js` `loadState` 中 `verLabel` 改为 `STATE.version.major`（仅显示 `v1.0.1`），并为 `copyLabel` 绑定 `onclick` 调用后端 `open_external`。`src/api_bridge.py` 新增 `open_external(url)` 方法，使用已导入的 `webbrowser.open` 打开系统浏览器。
- 验证：`node --check ui/app.js` 通过；`py_compile src/api_bridge.py` 通过；build.py 用 weibobot venv 重打包至 2609060003（v1.0.1）。旧版 2609060002 自动归档到 history/。
- 说明：`app.py` 中 `webview.settings["OPEN_EXTERNAL_LINKS_IN_BROWSER"] = False` 是为了防止微博登录页弹外部浏览器，所以 GitHub 链接不采用 `<a target="_blank">`，而是经后端 `webbrowser.open` 调用系统浏览器打开，避免被 pywebview 拦截为内部加载。

## 监控列表与概览优化（内部 2609060004）
- 需求（基于 3 张截图反馈）：① 监控列表筛选后无法清空，必须等刷新状态才能恢复显示；② 最近一次轮询结果原始 JSON 太乱，参考运行日志模式展示；③ 监控列表分页控件应放在列表上方，不用每次拉到底部翻页。
- 改动：`ui/app.js` 修复 `applyMonFilter()` 与 `clearMonFilters()`：之前只调用 `renderMonPage()`，没有重新计算 `MON_FILTERED`，导致筛选/清空后仍显示旧数据；改为调用 `renderMonitors()`，重新排序并计算筛选。新增 `formatLastResult(res, ts)`，将 `sch.last_result` 输出为 `[时间][INFO/ERROR] 内容` 的日志格式，按 action 优先级排序。`renderOverview()` 中 `#lastResult` 不再 `JSON.stringify`，改为 `formatLastResult`。`ui/index.html` 将 `#monPagination` 从 `#monList` 底部移到其上方，并增加 `mon-pagination-top` class；`#lastResult` class 改为 `log-box`。`ui/style.css` 新增 `.mon-pagination-top` 样式（底部边框、去顶部边距）。
- 验证：`node --check ui/app.js` 通过；build.py 用 weibobot venv 重打包至 2609060004（v1.0.1）。旧版 2609060003 自动归档到 history/。
- 结论：筛选清空立即生效；翻页控件在列表上方；概览轮询结果清晰可读。

## 数据总览页 + 概览细节优化（内部 2609060005）
- 需求：① 控制「最近一次轮询结果」高度不超过单页；② 系统概览「转发次数」改为「本日转发次数」；③ 在历史记录与软件授权之间新增「数据总览」页，包含本日/本月抓取转发、高频被转发、曲线图、导出。
- 改动：`ui/style.css` 新增 `.last-result-box`（max-height: 220px、overflow-y auto）；`ui/index.html` `#lastResult` 添加该 class。`ui/app.js` 概览统计标签改为「本日转发次数」并使用 `s.forwards_today`。新增左侧导航「数据总览」及其 `view-dataoverview` 区块（核心指标卡、趋势曲线、TOP10 高频对象、导出按钮）。`src/history_db.py` 在 `stats()` 增加 `forwards_today`，新增 `data_overview()` 聚合多维度统计与曲线序列（今日小时、本月天、本年月、全部年月）。`src/api_bridge.py` 新增 `get_data_overview()` / `export_data_overview(fmt)`。`ui/app.js` 新增 `loadDataOverview`、`renderDataOverview`、`drawLineChart`（纯 SVG 双曲线：抓取橙色、转发绿色）、标签切换、导出调用。
- 验证：`node --check ui/app.js` 通过；`py_compile src/history_db.py src/api_bridge.py` 通过；build.py 重打包至 2609060005（v1.0.1）。旧版 2609060004 自动归档到 history/。
- 导出说明：JSON 导出完整 `summary` + `series` + `top_forwarded`，CSV 导出汇总表（today/month/year/all 的抓取/转发），方便喂给 AI 分析。
