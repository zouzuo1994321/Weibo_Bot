# 微博bot小助手 运行与打包依赖
# 安装：pip install -r requirements.txt
pywebview>=6.0          # 原生窗口（Edge WebView2 内核），不依赖外部浏览器
requests>=2.28          # HTTP 请求（微博接口）
jieba>=0.42             # 中文分词（AI 轻量生成器关键词抽取）
Pillow>=10.0            # 图片处理（历史存档）
cryptography>=42.0      # Cookie 本地加密存储
PyInstaller>=6.0        # 打包为独立 exe
# 可选：导出 XLSX 需要 openpyxl
# openpyxl>=3.1
